create user account
    mongoose
    schema
    model
    usercreate -> password -> hash
    jwt token -> cookie

    login -> token -> decrypt -> email
    
    /jwt-auth-mongo-backend-main
│
├── /tests
│   └── user.test.js
│
├── /models
│   └── user.js
│
└── app.js


